function gzh(){
	var urls = [
    ];

    var url = urls[Math.floor(Math.random() * urls.length)];
    top.location.href=url;
}

function ad1(){
	var urls = [
    ];

    var url = urls[Math.floor(Math.random() * urls.length)];
    top.location.href=url;
}

function ad2(){
	var urls = [
    ];

    var url = urls[Math.floor(Math.random() * urls.length)];
    top.location.href=url;
}

function yw(){
	var urls = [
    ];

    var url = urls[Math.floor(Math.random() * urls.length)];
    top.location.href=url;
}